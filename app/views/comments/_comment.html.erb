<p>
    <strong><%= comment.commenter %></strong> said:
    "<%= comment.body %>"<br><br>
    <em>Posted <%= time_ago_in_words(comment.created_at) %> ago</em> | <%= link_to 'Destroy Comment', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure you want to delete this comment?' } %>
</p>